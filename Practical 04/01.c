#include<stdio.h>
int main()
{
    int no,odd,even;
    printf("enter intger:");
    scanf("%d",&no);
    if(no%2==0)
        printf("The number is even number");
    else
        printf("Enter number is odd number");
}
