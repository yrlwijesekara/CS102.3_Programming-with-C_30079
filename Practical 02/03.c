#include <stdio.h>
int main()
{
 int avg,dist,time;
 printf("Enter distance travelled");
 scanf("%d",dist);
 printf("Enter time taken");
 scanf("%d",time);
 avg=dist/time;
 printf("The average is %d",avg);
}
